# Copia questo file in `.env` e inserisci la tua chiave.
# `.env` e' gitignored: la chiave non finisce mai nel repo ne' nell'immagine.
# Docker Compose lo legge da solo.

DEEPSEEK_API_KEY=sk-la-tua-chiave-qui
